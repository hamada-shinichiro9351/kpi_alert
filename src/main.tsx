import React from 'react'
import ReactDOM from 'react-dom/client'
import KPIAlertDashboard from './KPIAlertDashboard.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <KPIAlertDashboard />
  </React.StrictMode>,
)
